export interface INpmRegistry {
    url: string;
    auth: string;
    authOnly: boolean;
}
export declare class NpmRegistry implements INpmRegistry {
    url: string;
    auth: string;
    authOnly: boolean;
    constructor(url: string, auth: string, authOnly?: boolean);
    static FromServiceEndpoint(endpointId: string, authOnly?: boolean): Promise<NpmRegistry>;
    private static isEndpointInternal;
    static FromFeedId(packagingUri: string, feedId: string, project: string, authOnly?: boolean, useSession?: boolean): Promise<NpmRegistry>;
    static FromUrl(url: string, authOnly?: boolean): NpmRegistry;
}
